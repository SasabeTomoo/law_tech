<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
<%= form_with(model: @damage, local: true) do |form| %>
  <% if @damage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :idolname %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<p id="notice"><%= notice %></p>
<%= form_with( model: @damages, method: :get, local: true ) do |form| %>
  <div class="content">
    <h8>プルダウン検索</h8>
    <p><%= form.select :search, {'1.床（畳、フローリング、カーペットなど）': '1.床（畳、フローリング、カーペットなど）', '2.壁、天井': '2.壁、天井', '3.建具等、襖（ふすま）、天井': '3.建具等、襖（ふすま）、柱等', '4.その他': '4.その他'}, { include_blank: '選択してください'}, { class: 'form-control' } %></p>
  </div>
  <%= form.submit '検索', name: nil%>
<% end %>

<p><%= notice %></p>
<table>
  <tr>
    <th>通常損耗の具体例</th>
    <th colspan="3"></th>
  </tr>
  <% @usual_samples.each do |usual_sample| %>
  <tr>
    <td class="task_row"><%= usual_sample.content %></td>
  </tr>
<% end %>
</table>
<table>
  <tr>
    <th>超過損耗の具体例</th>
    <th colspan="3"></th>
  </tr>
  <% @unusual_samples.each do |unusual_sample| %>
  <tr>
    <td class="task_row"><%= unusual_sample.content %></td>
  </tr>
<% end %>
</table>
