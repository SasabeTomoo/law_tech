<h1>管理画面</h1>
<h2>具体例のインポート</h2>
<div class="new_order_form_submit2"><%= link_to "インポート", samples_path, class: 'btn common_btn btn_shadow_dark' %></div>
<h2>ユーザー関係</h2>
<!-- <div class="new_order_form_submit2"><%= link_to 'user管理　', new_admin_user_path, class: 'btn common_btn btn_shadow_dark' %></div> -->
<table>
  <tr>
    <th>id</th>
    <th>名前</th>
    <!-- <th>email</th> -->
    <!-- <th>精算書入力回数</th> -->
    <th>管理権</th>
    <th colspan="3"></th>
  </tr>

 <% @users.each do |user| %>
 <tr>
   <td class="users"><%= user.id %></td>
   <td class="users"><%= user.name %></td>
   <!-- <td class="users"><%= Adjustment.where(user_id:user).size %></td> -->
   <td class="users"><%= user.admin %></td>
   <td class="new_order_form_submit2"><%= link_to "編集する", edit_admin_user_path(user.id), class: 'btn common_btn2 btn_shadow_dark' %></td>
   <td class="new_order_form_submit2"><%= link_to '削除する', admin_user_path(user.id), method: :delete , class: 'btn common_btn2 btn_shadow_dark', data: { confirm: '本当に削除していいですか？'} %></td>
 </tr>
<% end %>
</table>
